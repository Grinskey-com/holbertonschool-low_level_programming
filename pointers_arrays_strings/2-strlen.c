#include "main.h"
/**
 * _strlen - returns the length of a string
 * @s: pointer to the string to check
 *
 * Return: the length of the string as an integer
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
		/* Loop increments i until it hits '\0'*/
	}

	return (i);
}
